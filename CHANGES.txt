StoryTuner August 14 polish update

Changed files:
- components/activities/checkpoint-test.tsx
  * Foundations Check now keeps a purple identity in the test UI and completed state.
  * Foundations answer selections use purple instead of the global blue treatment.
- components/activities/activities-client.tsx
  * Removed the vertical accent strip from unit-test cards.
  * Completed Foundations Check icon now uses the purple test color.
- components/profile/profile-client.tsx
  * Profile stat now shows current spendable XP as "XP available" instead of lifetime Total XP.
- components/profile/shop-client.tsx
  * Removed decorative orbit circles behind Parch.
  * Equipping an owned Parch now shows the actual Parch in a centered circular confirmation with its name underneath, instead of confetti/text only.
- components/profile/settings-client.tsx
  * Display-name save check is grey with a white check when unchanged, blue when a change is ready.
  * Invalid display-name notice is red.
- app/globals.css
  * Removed the spiral/ripple pattern from the onboarding book cover and replaced it with a much subtler hardcover treatment.

Validation:
- All 17 backend tests pass.
- Changed TS/TSX files were syntax-transpiled successfully.
- Full project typecheck was not available in this environment because project dependencies/node_modules are not installed here.
