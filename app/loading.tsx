export default function Loading() {
  return (
    <main className="book-app mx-auto min-h-svh w-full max-w-md bg-background px-5 pt-[max(1.5rem,env(safe-area-inset-top))] text-foreground">
      <div className="font-mono text-[0.62rem] uppercase tracking-[0.18em] text-muted-foreground">StoryTuner</div>
    </main>
  )
}
